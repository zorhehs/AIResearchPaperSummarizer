# Example paper

`paper.pdf` is a copy of `tests/sample_papers/paper3.pdf` already supplied with this repository.

`summary.json` is the unedited response from the local `/summarize/stream` pipeline, generated on 2026-09-10. It contains extracted source text and page spans, the AI-generated summary, and server-verified citation metadata. The UI labels this as a saved example; opening it does not call the summarization provider or spend a summary credit. Chat questions still use the normal live backend.
