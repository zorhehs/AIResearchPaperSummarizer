version: "3.9"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
      - UNPAYWALL_EMAIL=${UNPAYWALL_EMAIL:-}
    volumes:
      - app-data:/app/data
    restart: unless-stopped

volumes:
  app-data:
